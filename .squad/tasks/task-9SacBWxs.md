---
id: task-9SacBWxs
title: Add input validation and sanitization to cron job and escalation config forms
status: delegated
priority: P1
assignee: leela
labels:
  - innovation
  - security
created: '2026-04-02T00:23:07.102Z'
updated: '2026-04-02T02:06:15.562Z'
sortIndex: 529
---
The cron page handleSubmit (cron/page.tsx ~L426) passes FormData values directly to mutate with no null checks (form.get() can return null → .trim() throws), no length limits (message/description can be megabytes), no cron expression validation, and no check that participant agent IDs actually exist. The knowledge ingestion handler (ingestion-status.tsx ~L63) has multiple overlapping setInterval/setTimeout calls with no cleanup on unmount — both timeouts fire causing duplicate state updates. Fix: add Zod schema validation on form submit, validate cron expressions before sending, add max-length constraints, and refactor ingestion to use a single AbortController-based polling loop.

_Auto-generated by Innovation Mode (security)_
ed by Innovation Mode (security)_



  ---

  **[2026-04-02 00:25:31]** ⚠️ Task was stuck in-progress after server restart.
  Reset to pending.
---
The cron page handleSubmit (cron/page.tsx ~L426) passes FormData values directly to mutate with no null checks (form.get() can return null → .trim() throws), no length limits (message/description can be megabytes), no cron expression validation, and no check that participant agent IDs actually exist. The knowledge ingestion handler (ingestion-status.tsx ~L63) has multiple overlapping setInterval/setTimeout calls with no cleanup on unmount — both timeouts fire causing duplicate state updates. Fix: add Zod schema validation on form submit, validate cron expressions before sending, add max-length constraints, and refactor ingestion to use a single AbortController-based polling loop.

_Auto-generated by Innovation Mode (security)_

---
**[2026-04-02 02:06:15]** 📋 Leela broke this task into 3 sub-tasks:

- **Frontend form validation and ingestion refactor** → Fry (Frontend Dev)
- **Backend cron API validation hardening** → Bender (Backend Dev)
- **Test validation and polling fixes** → Zoidberg (Tester)
**Feature Branch:** `feature/task-9SacBWxs`


**[2026-04-02 02:06:15]** 🔀 Task delegated — waiting for subtask completion.
