---
id: task-c14GSoA7
title: Eliminate N+1 API fetches and add missing useMemo on list pages
status: delegated
priority: P2
assignee: leela
labels:
  - innovation
  - performance
created: '2026-04-02T00:23:07.353Z'
updated: '2026-04-02T02:19:25.653Z'
sortIndex: 530
---
Skills page (skills/page.tsx) fetches all skills twice — once filtered, once unfiltered — just to build an ID set. Decisions page runs two parallel queries (all + search) instead of client-side filtering. Team member detail (team-members/[id]/page.tsx) fetches ALL tasks system-wide to filter by one assignee — should use server-side ?assignee= filter. Workflow detail (workflows/[id]/page.tsx ~L148) does O(n²) node lookup via .find() inside a .map() loop. GitHub page (github/page.tsx ~L19) computes linkedIssueNumbers inline without useMemo. Fix: deduplicate skill fetches, add server-side task filtering, memoize expensive computations, and build a node lookup Map for O(1) access.

_Auto-generated by Innovation Mode (performance)_
by Innovation Mode (performance)_



  ---

  **[2026-04-02 00:25:31]** ⚠️ Task was stuck in-progress after server restart.
  Reset to pending.
---
Skills page (skills/page.tsx) fetches all skills twice — once filtered, once unfiltered — just to build an ID set. Decisions page runs two parallel queries (all + search) instead of client-side filtering. Team member detail (team-members/[id]/page.tsx) fetches ALL tasks system-wide to filter by one assignee — should use server-side ?assignee= filter. Workflow detail (workflows/[id]/page.tsx ~L148) does O(n²) node lookup via .find() inside a .map() loop. GitHub page (github/page.tsx ~L19) computes linkedIssueNumbers inline without useMemo. Fix: deduplicate skill fetches, add server-side task filtering, memoize expensive computations, and build a node lookup Map for O(1) access.

_Auto-generated by Innovation Mode (performance)_

---
**[2026-04-02 02:19:25]** 📋 Leela broke this task into 2 sub-tasks:

- **Add server-side assignee filter to tasks API** → Bender (Backend Dev)
- **Fix N+1 fetches and add memoization across list pages** → Fry (Frontend Dev)
**Feature Branch:** `feature/task-c14GSoA7`


**[2026-04-02 02:19:25]** 🔀 Task delegated — waiting for subtask completion.
