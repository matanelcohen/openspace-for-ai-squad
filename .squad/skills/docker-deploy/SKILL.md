---
name: "Docker Deploy"
description: "Build, tag, and deploy Docker containers. Supports multi-stage builds, compose files, and registry push."
tags: ["docker", "containers", "deploy", "devops", "ci-cd"]
agentMatch:
  roles: ["*"]
requires:
  bins: []
  env: []
---

# Docker Deploy

Build, tag, and deploy Docker containers. Supports multi-stage builds, compose files, and registry push.